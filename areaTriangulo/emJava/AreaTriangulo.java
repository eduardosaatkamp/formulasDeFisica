package areaTriangulo.emJava;

public class AreaTriangulo {
    public static void main(String[] args) {
     
    double base = 10.0;
    double altura = 5.0;

    double area = 0.5 * base * altura;
    // ou area = (base * altura)/2

    System.out.println("The area of the triangle is: " + area);
    }
}
